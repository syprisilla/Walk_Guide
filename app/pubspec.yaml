name: walk_guide
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  # 기본 의존성
  cupertino_icons: ^1.0.8
  pedometer: ^4.1.1
  sensors_plus: ^6.1.1
  permission_handler: ^12.0.0+1

  # MLKit 및 카메라 관련 의존성 (추가됨)
  camera: ^0.10.5+9
  google_mlkit_object_detection: ^0.11.0
  google_mlkit_commons: ^0.6.0
  isolate: '>=2.1.1 <4.0.0'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  assets:
   - assets/images/
  # assets: 등은 필요 시 여기에 추가